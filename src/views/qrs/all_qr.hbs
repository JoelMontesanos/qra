<div class="row">
    {{#each qrs }}
        <div class="col-md-4">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title d-flex justify-content-between align-items-center">
                        {{nombre}} <a href="/qr/edit/{{_id}}">✔</i></a>
                        </h4>
                    <p>{{ine}}</p>
                    <p>{{auto}}</p>
                    <p>{{placas}}</p>
                    <p>{{motiva}}</p>
                    <form action="/qr/delete/{{_id}}" method="POST">
                        <button type="submit" class="btn btn-danger btn-block btn-sm"> Borrar </button>
                    </form>
                </div>
            </div><br>
        </div>
    {{/each}}
</div>